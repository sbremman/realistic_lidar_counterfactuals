from setuptools import setup, find_packages

setup(
    name="realistic_lidar_counterfactuals",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # List dependencies here, e.g., "numpy", "torch"
    ],
    author="Sindre Benjamin Remman",
    description="Package for generating realistic lidar counterfactuals.",
)
